name: fastapi-chatbot-infrastructure
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
description: FastAPI Chatbot Infrastructure on AWS
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python